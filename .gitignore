node_modules/
chrome_profile/
*.har
.DS_Store
logs/
curl_request.txt

# Zalo Bot Sessions (mỗi môi trường có session riêng — không commit)
zalo-session.json
zalo-session-staging.json
public/zalo-qr.png
data/

# Environment variables & secrets (.env.example được commit, .env thì không)
.env
.env.staging
.env.production

# DB Dumps
*.sql
*.dump

# Frontend build output
Affiliate-AI/client/dist/
Building Interactive Referral Genealogy.md

# UI reference files (local only)
thamkhaoUI/
